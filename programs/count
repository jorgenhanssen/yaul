// User inputs a number to count to (reg 0)
in 0

// Loop variables
set '0' 1   // current value
set '1' 2   // increment

// Loop and print the current value
LOOP:
add 2 1 1      // increment current value
out 1          // print the current value
jlt 1 0 LOOP   // if current value < target, re-loop